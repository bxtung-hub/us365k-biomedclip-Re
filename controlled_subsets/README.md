# Regenerated controlled subsets

These six JSONL files are deterministic regenerated manifests used for reproducibility and provenance. They are **not claimed to be byte-identical copies of the historical controlled-subset files**, which were not separately preserved.

Each file is gzip-compressed for GitHub upload. `SHA256_UNCOMPRESSED.txt` records the SHA256 of the uncompressed JSONL.
