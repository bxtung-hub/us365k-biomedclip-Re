# Environment records

These are preserved environment snapshots associated with the historical project/audit. The original projection-tuning requirements recorded `open_clip_torch` without pinning an exact version. Therefore, no exact OpenCLIP version is retroactively assigned to every historical FT run.
